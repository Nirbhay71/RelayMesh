Add create contact button in the main page after login in successfully. It will open the create contact form and will take input username, email address. It will validate all fields before submitting it to server side for further processing. It will also display error message if any field is not filled properly or entered wrong value. It should check email address with name exists in DB or not. If exists then only create the new contact and add the entry into the DB table. After successful creation of a new contact, it will redirect to main page after successful insertion of data into database and then show contact list on main page just like whatsapp app does. Whenever that user logged in successfully from any browser or mobile device, it should display all contacts created by this particular logged-in user. It should also have a delete button for each individual contact entry to be displayed on the screen of the web application. It will open up a confirmation dialog box which will ask if you are sure that you want to delete this particular contact from your list of all contacts created by this particular logged-in user. If yes then it will proceed with deletion of data from database table and also remove the entry from the DB table for further processing on server side. If no, then it should not perform any action or process at client side. When clicked on the contact from list of all contacts it should open the chat window in which you can send message to this particular user just like we do with whatsApp app. 

Now let me explain how I have implemented above mentioned functionality into my web application. First create the database schema and show me the Database schema. when creating new Contact check the email and name exists in users table or not if yes then then only add the entry to DB table for further processing on server side. If no, then it should not perform any action or process at client side. After adding to Contact table that contact should be visible instantly into contact list in frontend. 

When clicked on the contact from list of all contacts it should open the chat window in which you can send message to this particular user just like we do with whatsApp app. The message window should work in real time and should show up new messages as soon as they are received by server side for further processing. Also want to enable the horizontal scalling to make it true distributed system project. The message send to a contact should go to backend server and then backend server to the Redis pub/sub and from that Redis pub/sub to all backend servers and backend server checks if the mentioned contact is connected with that particular server or not if connected then sends the message. Ofcourse this all can be done with the help of the @socket.io/redis-adapter package which will provide us a lot of flexibility in our web application project. 

Whenever any contact starts typing then it should show typing just like whatsapp does. 

Also show the timestapm  when message was sent to that particular user.

Add single tick, double tick, blue double tick just like whatsapp does with the same meaning and functionality.
